# DocBox public assets

Upload these files to this folder on branch `agent/docbox-landing-performance`:

- `occu-med-logo.png`
- `docbox-landing.png`

The app reads them at `/occu-med-logo.png` and `/docbox-landing.png`.
